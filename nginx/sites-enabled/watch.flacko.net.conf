# HTTP → HTTPS
server {
    listen 80;
    listen [::]:80;
    server_name watch.flacko.net;
    return 301 https://$host$request_uri;
}

# HTTPS reverse-proxy to Jellyfin
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name watch.flacko.net;

    ssl_certificate     /etc/letsencrypt/live/watch.flacko.net/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/watch.flacko.net/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://10.0.0.149:8096;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header   Upgrade           $http_upgrade;
        proxy_set_header   Connection        "upgrade";
    }
}
